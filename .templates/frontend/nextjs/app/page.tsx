/**
 * Home page — intentionally empty.
 *
 * The agent generates page content during /build based on what was
 * discovered in /brainstorm and /define. Use the primitives in
 * components/primitives/ and the useStream() hook to build your UI.
 */

export default function HomePage() {
  return (
    <main>
      {/* Agent generates page content during /build */}
    </main>
  )
}
